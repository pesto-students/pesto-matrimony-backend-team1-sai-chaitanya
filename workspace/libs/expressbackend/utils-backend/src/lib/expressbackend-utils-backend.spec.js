import { expressbackendUtilsBackend } from './expressbackend-utils-backend';
describe('expressbackendUtilsBackend', () => {
  it('should work', () => {
    expect(expressbackendUtilsBackend()).toEqual(
      'expressbackend-utils-backend'
    );
  });
});
